services:
  - type: web
    name: greenfind-api
    runtime: python
    buildCommand: pip install -r backend/requirements.txt && cd backend && python build_db.py
    startCommand: cd backend && gunicorn app:app -w 1 -k uvicorn.workers.UvicornWorker --bind 0.0.0.0:$PORT
    plan: free
